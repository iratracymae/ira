import { FilePosition } from './input.js'

declare namespace CssSyntaxError {
  /**
   * A position that is part of a range.
   */
  export interface RangePosition {
    /**
     * The column number in the input.
     */
    column: number

    /**
     * The line number in the input.
     */
    line: number
  }

  // eslint-disable-next-line @typescript-eslint/no-use-before-define
  export { CssSyntaxError_ as default }
}

/**
 * The CSS parser throws this error for broken CSS.
 *
 * Custom parsers can throw this error for broken custom syntax using
 * the `Node#error` method.
 *
 * PostCSS will use the input source map to detect the original error location.
 * If you wrote a Sass file, compiled it to CSS and then parsed it with PostCSS,
 * PostCSS will show the original position in the Sass file.
 *
 * If you need the position in the PostCSS input
 * (e.g., to debug the previous compiler), use `error.input.file`.
 *
 * ```js
 * // Raising error from plugin
 * throw node.error('Unknown variable', { plugin: 'postcss-vars' })
 * ```
 *
 * ```js
 * // Catching and checking syntax error
 * try {
 *   postcss.parse('a{')
 * } catch (error) {
 *   if (error.name === 'CssSyntaxError') {
 *     error //=> CssSyntaxError
 *   }
 * }
 * ```
 */
declare class CssSyntaxError_ {
  /**
   * Source column of the error.
   *
   * ```js
   * error.column       //=> 1
   * error.input.column //=> 4
   * ```
   *
   * PostCSS will use the input source map to detect the original location.
   * If you need the position in the PostCSS input, use `error.input.column`.
   */
  column?: number

  /**
   * Source column of the error's end, exclusive. Provided if the error pertains
   * to a range.
   *
   * ```js
   * error.endColumn       //=> 1
   * error.input.endColumn //=> 4
   * ```
   *
   * PostCSS will use the input source map to detect the original location.
   * If you need the position in the PostCSS input, use `error.input.endColumn`.
   */
  endColumn?: number

  /**
   * Source line of the error's end, exclusive. Provided if the error pertains
   * to a range.
   *
   * ```js
   * error.endLine       //=> 3
   * error.input.endLine //=> 4
   * ```
   *
   * PostCSS will use the input source map to detect the original location.
   * If you need the position in the PostCSS input, use `error.input.endLine`.
   */
  endLine?: number

  /**
   * Absolute path to the broken file.
   *
   * ```js
   * error.file       //=> 'a.sass'
   * error.input.file //=> 'a.css'
   * ```
   *
   * PostCSS will use the input source map to detect the original location.
   * If you need the position in the PostCSS input, use `error.input.file`.
   */
  file?: string

  /**
   * Input object with PostCSS internal information
   * about input file. If input has source map
   * from previous tool, PostCSS will use origin
   * (for example, Sass) source. You can use this
   * object to get PostCSS input source.
   *
   * ```js
   * error.input.file //=> 'a.css'
   * error.file       //=> 'a.sass'
   * ```
   */
  input?: FilePosition

  /**
   * Source line of the error.
   *
   * ```js
   * error.line       //=> 2
   * error.input.line //=> 4
   * ```
   *
   * PostCSS will use the input source map to detect the original location.
   * If you need the position in the PostCSS input, use `error.input.line`.
   */
  line?: number

  /**
   * Full error text in the GNU error format
   * with plugin, file, line and column.
   *
   * ```js
   * error.message //=> 'a.css:1:1: Unclosed block'
   * ```
   */
  message: string

  /**
   * Always equal to `'CssSyntaxError'`. You should always check error type
   * by `error.name === 'CssSyntaxError'`
   * instead of `error instanceof CssSyntaxError`,
   * because npm could have several PostCSS versions.
   *
   * ```js
   * if (error.name === 'CssSyntaxError') {
   *   error //=> CssSyntaxError
   * }
   * ```
   */
  name: 'CssSyntaxError'

  /**
   * Plugin name, if error came from plugin.
   *
   * ```js
   * error.plugin //=> 'postcss-vars'
   * ```
   */
  plugin?: string

  /**
   * Error message.
   *
   * ``e användbarSäkerhetsinformationen som delas är inte användbarJag får för många aviseringarJag ser hur Microsoft Edge använder enhetens processor och minne optimaltJag gillar hur Microsoft Edge hjälper att hålla mig säkrare när jag är onlineBerätta varför du är nöjd:Är du nöjd med Browser Essentials?RewardsIE-OMAKTIVERINGSDIALOGDen här webbplatsen fungerar inte med Internet Explorer!Den här webbplatsen fungerar bättre i Microsoft EdgeMicrosoft rekommenderar att du fortsätter i Microsoft Edge för att få bättre hastighet, prestanda och säkerhet.Du surfar nu i Microsoft EdgeUndvik avbrott genom att föra in webbdata och inställningar i Internet Explorer när detta händerVill du stänga InPrivate-fönster?InPrivate-fönster måste stängas för att du ska få åtkomst till Inställningar. Vill du stänga dem nu?Autentiseringsuppgifter krävsOm du vill aktivera synkning på Xbox måste du ange din PIN-kod.Alternativ för kommandopalettenOm du vill stänga fönstret klickar du utanför fönstret eller väljer ESC-tangenten.Ange "större än"-tecknet för att hämta en lista med kommandon eller ange "frågetecken"-tecknet för att visa hjälp i popup-listrutanLäs mer om experimentella funktionerLämna feedback i feedbackverktyget (ALT + SKIFT + I) för att berätta vad du tycker.FörslagFlikarGruppDevToolsÖppna bokmärke…PanelMobilKonsolFelsökningsprogramVisa konsolVisa elementVisa nätverkVäxla enhetsemuleringVisa Välkommen efter varje uppdateringVäxla till systemets föredragna färgtemaRensa konsolhistorikInaktivera JavaScriptVisa kodfragmentÖppna dedikerade DevTools för NodVisa källorSök $1 efter "$2"Öppna GitHubÖppna Stack OverflowÖppna Outlook Web AppÖppna Office webbappÖppna TeamsÖppna temaarkivetÖppna tilläggsarkivetVisa historikSkapa ny flikÖppna nyligen stängd flikSkapa ett nytt fönsterSkapa nytt webbläsarfönster för InPrivate-surfningBokmärk den här flikenBokmärk alla flikarZooma utÅterställ zoomningVisa sidkällaPlats för e-postsidaFokusplatsfältVerktygsfältet FokusVisa aktivitetshanterarenVisa bokmärkeshanterarenVisa nedladdningarVisa inställningarVäxla profilVäxla utvecklarverktygÖppna i webbappVisa ApphanterarenVisa menyn ApparSök flikar …Nästa flikFöregående flikFlytta fliken framåtFlytta flik bakåtAutentisera dig själv med ditt primära lösenordPrimärt lösenordAutentiseringen m